import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: '90%',
  },
  button: {
    marginTop: 80,
    borderRadius: 19,
    alignSelf: 'center'
  }
})

export default styles